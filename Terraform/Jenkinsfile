pipeline {
    agent any
    stages {
        stage(executing terraform script) {
            withCredentials([amazonWebServicesCredentials(credentialsId: 'AWS-1', accessKeyVariable:'AWS_ACCESS_KEY_ID', secretKeyVariable:'AWS_SECRET_ACCESS_KEY')])
            steps{
                sh '''
                terraform init
                terraform plan
                '''

            }
        }
    }
}
